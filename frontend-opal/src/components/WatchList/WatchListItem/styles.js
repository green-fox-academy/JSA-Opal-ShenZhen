import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  border: {
    borderWidth: 2,
    borderStyle: 'solid',
    borderColor: 'black',
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between'
  }
});
