import { StyleSheet } from 'react-native';

import tools from 'components/common/tools';

export default StyleSheet.create({
  container: {
    marginLeft: tools.getPixel(20),
    marginTop: tools.getPixel(20),
    marginRight: tools.getPixel(10)
  }
});
